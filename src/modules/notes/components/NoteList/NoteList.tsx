import css from './NoteList.module.scss'
// import { useSelector } from 'react-redux'
// import { selectOpened, close, closeOthers } from '../../contexts/notes'
// import { useAppDispatch } from '@/redux.hooks'

export const NoteList = () => {
  // let opened = useSelector(selectOpened)
  // let dispatch = useAppDispatch()
  //
  // return (
  //   <div className={css.root}>
  //     <ul className={css.list}>
  //       {opened.map((note) => (
  //         <li className={css.item}>
  //           <span>{note}</span>
  //
  //           <button onClick={() => dispatch(closeOthers(note))}>⊙</button>
  //           <button onClick={() => dispatch(close(note))}>⨯</button>
  //         </li>
  //       ))}
  //     </ul>
  //   </div>
  // )
}
