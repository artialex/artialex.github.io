Properties of `equals()` method:

- Reflexivity
	- `a.equals(a) == true`
- Symmetry 
	- `a.equals(b) == b.equals(a)`
- Transitivity
	- `a.equals(b) && b.equals(c) => a.equals(c)`
- `a.equals(null) == false`

